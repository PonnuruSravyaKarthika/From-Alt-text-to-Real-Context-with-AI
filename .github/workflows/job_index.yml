name: strategy_job_index 
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [14, 16, 18]
    steps:
      - name: Show job index
        run: echo "Job index is ${{ strategy.job-index }}"
